import {Character} from '../Character';
import {Range} from '../../../numeric/ranges/Range';

export default function(domain: Range, inferrable?: boolean, distance?: boolean, label?: string, labelStates?: (s: Range) => string, stateLabels?: string[]): Character<Range>
{
    // :TODO:
    return null;    
}